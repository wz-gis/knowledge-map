# Inbox

临时收集区。

适合放：

- 还没分类的想法。
- 临时链接。
- 待整理代码片段。
- 对话中产生但还没沉淀的内容。

整理规则：

- 每周至少清理一次。
- 能形成项目的，移动到 `01_projects/`。
- 能长期复用的，移动到 `02_areas/` 或 `03_resources/`。
- 可执行命令或配置片段，移动到 `snippets/`。
- 过期但仍有历史价值的，移动到 `04_archive/`。
